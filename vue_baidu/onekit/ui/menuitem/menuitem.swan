<view
class="onekit-menuitem {{Class}}"
style="{{style}}" 
id="{{Id}}"
checked="{{checked}}"
command="{{command}}"
default="{{default}}"
disabled="{{disabled}}"
icon="{{icon}}"
label="{{label}}"
radiogroup="{{radiogroup}}"
type="{{type}}"
>
<slot/>
</view>